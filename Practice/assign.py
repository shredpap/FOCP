def find_min(a,b):
    if a>b:
        return b
    else:
        return a 
print(find_min(4,5))
'''
it defines a function called print_header(msg). returns the value provided by the ‘msg’ parameter to the screen. (prefixed by five asterisk ‘*****’) characters.
'''